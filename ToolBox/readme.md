# 🧰 DrawingToolbox.mq5

**DrawingToolbox** is a MetaTrader 5 indicator that adds a small **floating toolbox** directly on the chart, providing quick-access buttons to draw common graphical objects.

---

## ✨ Features

- Floating panel **centered horizontally** on the chart  
- Buttons for drawing:
  - `─` **Horizontal line**
  - `│` **Vertical line**
  - `╱` **Trend line**
  - `█` **Rectangle**
  - `TXT` **Text label**
  - `∙` **Dot (small arrow)**
  - `✘` **Clear all**
- Click a button → select the drawing tool  
- Click anywhere on the chart → draw the selected object  
- Click `✘` → remove all objects created by the toolbox (`TB_…`)  
- Automatically **re-centers** when the chart window is resized  

---

## 🪄 How to Use

1. Copy `Toolbox.mq5` into your MetaTrader 5 folder:  

